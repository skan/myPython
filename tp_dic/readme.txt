specifications
    an organized dictionnary
        through a class containing 2 lists
            list for keys
            list for values
        
        create an empty dico
        copy from an another dico myDico (otherDico)
        pre initialized myDico (key1=val1, key2=val2)
        if we delete a key, the val is deleted
        add a value myDico[key] = val 
        if value exist, update
        get value: myDico[key]
        del myDico[key] 
        "key in dico" returns true or false
        len (dico) returns number of values in dico
        method sort 
        methode reverse
        for loop: for val in dico allows to browse keys
        method keys
        method values
        method items()
        dico1 + dico2
